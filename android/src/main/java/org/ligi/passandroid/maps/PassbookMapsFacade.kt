package org.ligi.passandroid.maps

import android.content.Context

object PassbookMapsFacade {

    fun init(context: Context): Boolean = false

    fun startFullscreenMap(context: Context) {
    }

    fun hasSupport(): Boolean = false
}
