<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/top_app_bar"
        android:elevation="0dp"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/top_app_bar"
            app:navigationIconTint="@android:color/white"
            app:popupTheme="@style/PassWallet.ToolbarPopup"
            app:titleTextAppearance="@style/PassWallet.ToolbarTitle"
            app:titleTextColor="@android:color/white" />

        <com.google.android.material.tabs.TabLayout
            android:id="@+id/tab_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/top_app_bar"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            app:tabBackground="@android:color/transparent"
            app:tabIndicatorColor="@color/accent"
            app:tabIndicatorFullWidth="false"
            app:tabIndicatorGravity="bottom"
            app:tabIndicatorHeight="3dp"
            app:tabMinWidth="88dp"
            app:tabMode="scrollable"
            app:tabPaddingStart="16dp"
            app:tabPaddingEnd="16dp"
            app:tabSelectedTextColor="@color/tab_text_selected"
            app:tabTextAppearance="@style/PassWallet.TabText"
            app:tabTextColor="@color/tab_text" />

    </com.google.android.material.appbar.AppBarLayout>

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/view_pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

    <TextView
        android:id="@+id/emptyView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fontFamily="sans-serif"
        android:gravity="center"
        android:lineSpacingExtra="6dp"
        android:padding="32dp"
        android:text="@string/empty_text_title"
        android:textAlignment="center"
        android:textColor="?android:textColorPrimary"
        android:textSize="18sp" />

    <LinearLayout
        android:id="@+id/fam"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end|bottom"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="24dp"
        android:clipToPadding="false"
        android:gravity="end"
        android:orientation="vertical"
        app:layout_behavior="org.ligi.passandroid.ui.MyShyFABBehavior">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/fab_action_import"
            style="@style/PassWallet.FabMenuButton"
            android:layout_width="wrap_content"
            android:layout_height="52dp"
            android:text="@string/import_pass"
            android:visibility="gone"
            app:icon="@drawable/ic_file_folder_open"
            app:iconGravity="textStart" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/fab_action_create_pass"
            style="@style/PassWallet.FabMenuButton"
            android:layout_width="wrap_content"
            android:layout_height="52dp"
            android:text="@string/fab_choice_create_pass"
            android:visibility="gone"
            app:icon="@drawable/ic_editor_mode_edit"
            app:iconGravity="textStart" />

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fab_menu_toggle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end"
            android:layout_marginTop="4dp"
            android:contentDescription="@string/fab_choice_create_pass"
            android:src="@drawable/ic_add_24"
            app:backgroundTint="@color/accent"
            app:borderWidth="0dp"
            app:tint="@android:color/white" />

    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
